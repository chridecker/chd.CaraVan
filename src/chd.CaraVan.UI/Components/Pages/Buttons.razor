@page "/buttons"
@inject IPiManager manager

@using chd.CaraVan.Devices
@using chd.CaraVan.UI.Components.Pages.Cards
<SmartButton Checked="manager.Read(22)" CheckedChanged="Change"></SmartButton>
@code {
    private void Change(bool val)
    {
        manager.Write(22, val);
    }
}
